function R = Optimization(x)
Assign
Geo = get_Geometry(x);
Mission = get_Misssion(Mission); 
MDA(x, error) % MDA Coordinatoor (Converger)
Aero = get_Aero(Solver, n=1); % Aerodynamic Performance
L_D = 
[R, eta] = get_Performance(x, W, Ref, L_D); 
end